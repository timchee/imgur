export function singleTag(
    id,
    name,
    display_name,
    followers,
    total_items,
    background_hash
){
    let tag = `<div class="tag 
    text-white 
    rounded-sm 
    overflow-hidden 
    "  id="image-container" 
    data-id=${id}>`;
}